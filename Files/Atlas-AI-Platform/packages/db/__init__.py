"""Database infrastructure package.

`base.py` defines the SQLAlchemy declarative base, `session.py` owns engine and
session creation, and `models` contains ORM entities similar to EF Core models.
"""
